import 'package:flutter/material.dart';

class CardPets extends StatelessWidget {
  final String nome;

  const CardPets({Key key, this.nome}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}